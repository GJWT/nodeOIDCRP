OpenID Connect RP Example
=========================

An example usage of the oidc-rp library, using OpenID Connect Dynamic Discovery
and Registration.


To run the example app:

```
npm i
npm start
```

Point your browser to [http://localhost:3000/](http://localhost:3000/).
